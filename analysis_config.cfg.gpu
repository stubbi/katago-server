# KataGo Analysis Engine Configuration - GPU optimized (CUDA)
# For use with "katago analysis" command
# Optimized for RTX 3080 (10GB VRAM) on Salad Cloud

# Logging: to stderr only (captured by katago-server), no log files on disk
logToStderr = true
logSearchInfo = false
logAllRequests = false
logAllResponses = false

# Analysis behavior
reportAnalysisWinratesAs = SIDETOMOVE

# Threading configuration for GPU (optimized for RTX 3080)
# 2 analysis threads for concurrent request handling
numAnalysisThreads = 2
numSearchThreadsPerAnalysisThread = 4

# Neural network batching (RTX 3080 optimized)
# Batch size of 32 balances throughput and latency on 10GB GPUs
nnMaxBatchSize = 32
numNNServerThreadsPerModel = 2

# Performance tuning for GPU (RTX 3080 with 10GB VRAM)
nnCacheSizePowerOfTwo = 22  # ~4M cache entries (~256MB)

# Default max visits (can be overridden per-request)
# 1000 visits provides good analysis quality with ~500ms response time
maxVisits = 1000
